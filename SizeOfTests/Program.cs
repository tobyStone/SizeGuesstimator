namespace SizeGuesstimator
{
    using System;
    using Xunit;

    internal class Program
    {
        static void Main(string[] args)
        {
            // an experiment to see if we can estimate the size of objects so we can weight them in the cache.
            // Then can use this to view them as a treemap - http://philogb.github.io/jit/static/v20/Jit/Examples/Treemap/example1.html
            // Maybe options to show cached objects as larger based on;
            //      age, size, number of hits.


            //var x = new ClassWithJustASingleString { StringProperty = "123456789023424234234234234" };
        }
    }
}
